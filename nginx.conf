server {
    listen 80;
    server_name _;

    # Strip /uploads prefix if present, otherwise serve from /uploads root
    location / {
        rewrite ^/uploads/(.*)$ /$1 break;
        root /usr/share/nginx/html/uploads;
        try_files $uri $uri/ =404;
    }
}
